models:
  - name: stg_salesagent
    description: one unique order per row
    columns:
      - name: orderid
        description: the primary key for stg_salesagent
      - name: shipmode
        description: '{{ doc("salesagent_doc")}}'
